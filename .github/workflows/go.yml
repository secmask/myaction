name: Build and test Go
on: [push, pull_request]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.13
        uses: actions/setup-go@v1
        with:
          go-version: 1.13
      - name: Check out source code
        uses: actions/checkout@v1
      - name: Build
        env:
          GOPROXY: "https://proxy.golang.org"
        run: |
          cd cmd/myaction
          go build .
      - name: Test
        env:
          GOPROXY: "https://proxy.golang.org"
        run: |
          docker run --name mypg -v `pwd`/main.go:/var/lib/main.go -e POSTGRES_PASSWORD=secmask -d postgres
          go test -v ./...